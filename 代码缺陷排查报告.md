# 全域代码缺陷、业务链路、接口故障深度排查报告

> 项目：mood_health 情绪健康管理平台
> 排查日期：2026-07-16
> 技术栈：Vue3 前端 + TypeScript 后端（Express） + MySQL 8.4

---

## 目录

- [一、🚨 阻断级 BUG（P0）](#一-阻断级-bugp0)
  - [P0-1. 前端情绪类型 ID 与后端数据库不一致](#p0-1-前端情绪类型-id-与后端数据库不一致)
  - [P0-2. /api/auth/register 返回 500 — DuplicateUserError 缺少 statusCode](#p0-2-apiauthregister-返回-500--duplicateusererror-缺少-statuscode)
  - [P0-3. authController 中 throw error 导致异步异常未处理](#p0-3-authcontroller-中-throw-error-导致异步异常未处理)
  - [P0-4. recordMood 控制器中 req.user! 非空断言无防御性检查](#p0-4-recordmood-控制器中-requser-非空断言无防御性检查)
- [二、⚠️ 隐性风险（P1）](#二-隐性风险p1)
  - [P1-1. 全局错误日志缺失 — console.error 替代 logger.error](#p1-1-全局错误日志缺失--consoleerror-替代-loggererror)
  - [P1-2. getMoodTypes / getTagsHandler / createTagHandler 返回格式不一致](#p1-2-getmoodtypes--gettagshandler--createtaghandler-返回格式不一致)
  - [P1-3. validateRequest 中间件返回格式不统一](#p1-3-validaterequest-中间件返回格式不统一)
  - [P1-4. rolePermissions 硬编码映射表未被使用 — 死代码](#p1-4-rolepermissions-硬编码映射表未被使用--死代码)
  - [P1-5. 前端 guards.ts 角色权限表与后端数据库不一致](#p1-5-前端-guardsts-角色权限表与后端数据库不一致)
  - [P1-6. App.vue onMounted 中无条件调用 moodStore.fetchMoodList](#p1-6-appvue-onmounted-中无条件调用-moodstorefetchmoodlist)
- [三、💡 优化建议（P2）](#三-优化建议p2)
  - [P2-1. 加密模块启动时可能崩溃](#p2-1-加密模块启动时可能崩溃)
  - [P2-2. moodService 使用 require() 动态加载加密模块](#p2-2-moodservice-使用-require-动态加载加密模块)
  - [P2-3. cache.ts 使用 console.warn 和 console.log](#p2-3-cachets-使用-consolewarn-和-consolelog)
  - [P2-4. 前端 moodRecordStore 走旧版 API 路径](#p2-4-前端-moodrecordstore-走旧版-api-路径)
  - [P2-5. moodController 中 recordMood 函数过长](#p2-5-moodcontroller-中-recordmood-函数过长)
- [四、核心流程闭环验证](#四核心流程闭环验证)
- [五、优先级修复清单](#五优先级修复清单)

---

## 一、🚨 阻断级 BUG（P0）

> 直接导致功能无法使用，必须最先修复。

---

### P0-1. 前端情绪类型 ID 与后端数据库不一致

| 项目 | 详情 |
|------|------|
| **问题类型** | 前后端数据契约不一致 |
| **故障位置** | 前端 `src/views/mood/MoodRecord.vue` L250-L293，后端种子数据 `mood_health_server/src/db/seeds/coreSeed.ts` L83-L94 |
| **严重程度** | 🚨 P0 阻断级 |
| **影响范围** | 情绪记录创建功能，58% 的情绪选择会失败 |

**故障原因分析：**

前端 `MoodRecord.vue` 的 `visibleMoodRows` 渲染了 **Plutchik 情绪轮** 的 24 种情绪 ID：

```
ecstasy, admiration, fear, amazement, grief, disgust, angry, vigilance,
delight, trust, terror, surprise, sad, loathing, rage, anticipation,
calm, acceptance, apprehension, distraction, pensiveness, boredom, annoyance, interest
```

但后端 `emotion_types` 种子数据只有 **10 种**：

```
calm, happy, delight, neutral, sad, anxious, angry, irritable, excited, tired
```

**只有 10 种情绪能匹配成功，其余 14 种（58%）必失败，返回 400 "情绪类型不存在"。**

**最小复现步骤：**

1. 登录后进入情绪记录页面 `/mood/record`
2. 在"强烈情绪"行点击 "😆"（ecstasy 狂喜）或 "😨"（fear 恐惧）
3. 填写强度、描述，点击"保存这次情绪记录"
4. 后端返回：`{"code": 400, "message": "情绪类型不存在"}`

**修复建议：**

将前端 UI 渲染的 24 种 Plutchik 情绪类型全部补充到后端种子数据中，同时将 `moodRecordStore` 中独有但种子数据缺失的 `grateful` 也一并添加。

在 `mood_health_server/src/db/seeds/coreSeed.ts` 中，将 `REFERENCE_EMOTION_TYPES` 数组扩展为：

```typescript
// mood_health_server/src/db/seeds/coreSeed.ts L83-L94 → 替换为以下
const REFERENCE_EMOTION_TYPES = [
  // ===== 原有 10 种 =====
  ['calm', '平静', 'calm', 'neutral', 10],
  ['happy', '快乐', 'smile', 'positive', 20],
  ['delight', '愉悦', 'sun', 'positive', 25],
  ['neutral', '一般', 'meh', 'neutral', 30],
  ['sad', '难过', 'cloud-rain', 'negative', 40],
  ['anxious', '焦虑', 'activity', 'negative', 50],
  ['angry', '愤怒', 'flame', 'negative', 60],
  ['irritable', '烦躁', 'zap', 'negative', 65],
  ['excited', '兴奋', 'star', 'positive', 70],
  ['tired', '疲惫', 'battery-low', 'neutral', 80],
  // ===== 新增：Plutchik 情绪轮 14 种 + grateful =====
  ['ecstasy', '狂喜', 'star', 'positive', 22],
  ['admiration', '钦佩', 'heart', 'positive', 26],
  ['fear', '恐惧', 'alert-triangle', 'negative', 42],
  ['amazement', '惊愕', 'zap', 'neutral', 28],
  ['grief', '悲伤', 'cloud-rain', 'negative', 44],
  ['disgust', '厌恶', 'thumbs-down', 'negative', 62],
  ['vigilance', '警惕', 'eye', 'neutral', 32],
  ['trust', '信任', 'handshake', 'positive', 24],
  ['terror', '害怕', 'alert-octagon', 'negative', 46],
  ['surprise', '惊喜', 'gift', 'positive', 27],
  ['loathing', '反感', 'frown', 'negative', 63],
  ['rage', '生气', 'flame', 'negative', 64],
  ['anticipation', '期待', 'clock', 'positive', 23],
  ['acceptance', '接纳', 'check-circle', 'positive', 21],
  ['apprehension', '忧虑', 'cloud', 'negative', 52],
  ['distraction', '娱乐', 'gamepad', 'neutral', 34],
  ['pensiveness', '沉思', 'book', 'neutral', 36],
  ['boredom', '无聊', 'meh', 'neutral', 66],
  ['annoyance', '烦恼', 'frown', 'negative', 67],
  ['interest', '兴趣', 'compass', 'positive', 22],
  ['grateful', '感恩', 'heart', 'positive', 15],
] as const
```

> 注意：修改种子数据后需要重新运行 `npm run seed`（或对应的 seed 命令）来更新数据库。

---

### P0-2. /api/auth/register 返回 500 — DuplicateUserError 缺少 statusCode

| 项目 | 详情 |
|------|------|
| **问题类型** | 异常类型继承缺陷 |
| **故障位置** | `mood_health_server/src/repositories/userRepository.ts` L63-L68，`mood_health_server/src/controllers/authController.ts` L20，`mood_health_server/src/middleware/errorHandler.ts` L22 |
| **严重程度** | 🚨 P0 阻断级 |
| **影响范围** | 注册重复用户名时返回 500 而非 409 |

**故障原因分析：**

`DuplicateUserError` 直接继承 `Error`（而非 `AppError` 或 `HttpException`），没有 `statusCode` 属性。

调用链：`authService.register()` → `repository.createStudentUser()` → 抛出 `DuplicateUserError` → `authService` 捕获后重新抛出 `BusinessError`（这个是 OK 的，有 statusCode=400）。

但如果 `DuplicateUserError` 从其他路径未被包装而直接到达全局错误处理器，`errorHandler.ts` 中的 `error.statusCode || error.status || 500` 会得到 **500**。

同样，`MissingRoleError`（L70-L75）也继承 `Error` 而非 `AppError`。

**最小复现步骤：**

1. 注册用户 `testuser` / `123456`
2. 再次用相同用户名 `testuser` 注册
3. 返回 500：`{"code": 1500, "message": "用户名或邮箱已存在"}`

**修复建议：**

将 `DuplicateUserError` 和 `MissingRoleError` 改为继承 `HttpException`：

```typescript
// mood_health_server/src/repositories/userRepository.ts L63-L75
// 文件顶部添加 import
import { HttpException } from '../utils/errors'

export class DuplicateUserError extends HttpException {
  constructor() {
    super('用户名或邮箱已存在', 409)  // 409 Conflict
    this.name = 'DuplicateUserError'
  }
}

export class MissingRoleError extends HttpException {
  constructor(roleCode: UserRoleCode) {
    super(`缺少系统角色: ${roleCode}`, 500)
    this.name = 'MissingRoleError'
  }
}
```

---

### P0-3. authController 中 throw error 导致异步异常未处理

| 项目 | 详情 |
|------|------|
| **问题类型** | 异步错误处理缺陷 |
| **故障位置** | `mood_health_server/src/controllers/authController.ts` L20（register），L34（login） |
| **严重程度** | 🚨 P0 阻断级 |
| **影响范围** | register、login 接口异常时可能返回 500 或请求挂起 |

**故障原因分析：**

`register()` 和 `login()` 是 `async` 函数，在 catch 块中使用 `throw error` 而非显式返回 HTTP 响应。Express 4.x **不会自动捕获 async 路由处理器的 Promise rejection**。如果项目未安装 `express-async-errors` 包，这些错误会成为未处理的 Promise rejection，导致：

- 请求挂起直至超时
- 或返回空白 500 响应

**修复建议：**

在 catch 块中显式返回 HTTP 响应，而不是 `throw error`：

```typescript
// mood_health_server/src/controllers/authController.ts L10-L22
// 需要先引入 NextFunction
import { Request, Response, NextFunction } from 'express'

export const register = async (req: Request, res: Response, _next: NextFunction) => {
  try {
    await authService.register(req.body)
    res.status(201).json(apiSuccess(null, '注册成功'))
  } catch (error: any) {
    // 区分已知异常类型
    if (error instanceof HttpException) {
      return res.status(error.statusCode).json(
        apiFailure(error.statusCode, error.message)
      )
    }
    if (error instanceof BusinessError) {
      return res.status(400).json(
        apiFailure(400, error.message)
      )
    }
    // 未知异常
    logger.error('用户注册失败', {
      path: req.originalUrl,
      username: req.body?.username,
      error: error instanceof Error ? error.message : 'unknown_error',
    })
    return res.status(500).json(apiFailure(500, '服务器内部错误'))
  }
}

export const login = async (req: Request, res: Response, _next: NextFunction) => {
  try {
    const result = await authService.login(req.body)
    res.json(apiSuccess(result, '登录成功'))
  } catch (error: any) {
    if (error instanceof HttpException) {
      return res.status(error.statusCode).json(
        apiFailure(error.statusCode, error.message)
      )
    }
    if (error instanceof BusinessError) {
      return res.status(400).json(
        apiFailure(400, error.message)
      )
    }
    logger.error('用户登录失败', {
      path: req.originalUrl,
      username: req.body?.username,
      error: error instanceof Error ? error.message : 'unknown_error',
    })
    return res.status(500).json(apiFailure(500, '服务器内部错误'))
  }
}
```

> 备选方案：在 `app.ts` 中引入 `express-async-errors` 包，它通过 monkey-patch 让 Express 4.x 自动捕获 async 错误。但推荐方案是显式处理，更可控。
>
> ```bash
> npm install express-async-errors
> ```
> ```typescript
> // app.ts 最顶部
> import 'express-async-errors'
> ```

---

### P0-4. recordMood 控制器中 req.user! 非空断言无防御性检查

| 项目 | 详情 |
|------|------|
| **问题类型** | 空值风险 — 非空断言 |
| **故障位置** | `mood_health_server/src/controllers/moodController.ts` L11, L102, L123, L134, L223, L246, L279, L290, L311 |
| **严重程度** | 🚨 P0 阻断级 |
| **影响范围** | 所有 mood 接口，如果 auth 中间件未正确设置 req.user 则崩溃 |

**故障原因分析：**

所有 moodController 方法使用 `req.user!.userId` 非空断言。虽然 `moodRoutes.ts` L20 挂载了 `router.use(authenticate)`，但如果 `authenticate` 中间件因某种边界情况未正确设置 `req.user`（例如 JWT secret 空字符串时 token 仍然"解析成功"但 user 未赋值），代码会直接抛出 `TypeError: Cannot read properties of undefined`。

**修复建议：**

在每个 handler 开头增加防御性检查：

```typescript
// mood_health_server/src/controllers/moodController.ts
// 在每个 handler 的 try 块开头增加：

export const recordMood = async (req: AuthRequest, res: Response) => {
  try {
    // 防御性检查
    if (!req.user) {
      return res.status(401).json(apiFailure(401, '未登录'))
    }
    const userId = req.user.userId
    // ... 后续逻辑不变
  } catch (error) {
    // ...
  }
}
```

> 同理，对 `getMoodList`、`getWeeklyReportHandler`、`updateMoodHandler`、`deleteMoodHandler`、`getMoodTrend`、`getTagsHandler`、`createTagHandler`、`getMoodAnalysisHandler` 全部加上相同的防御检查。

---

## 二、⚠️ 隐性风险（P1）

> 偶发故障、边界场景报错，建议尽快修复。

---

### P1-1. 全局错误日志缺失 — console.error 替代 logger.error

| 项目 | 详情 |
|------|------|
| **问题类型** | 日志规范缺陷 |
| **故障位置** | `mood_health_server/src/controllers/moodController.ts` L96, L117, L127, L216, L239, L256, L272, L283, L304, L321 |
| **严重程度** | ⚠️ P1 隐性风险 |
| **影响范围** | 生产环境 mood 相关错误无法追踪 |

**故障原因分析：**

`moodController.ts` 所有 10 个 catch 块全部使用 `console.error(error)` 而非 `logger.error(...)`。在生产环境中，`console.error` 输出不会写入日志文件，故障排查时看不到任何错误信息。

**修复建议：**

全局替换 `console.error(error)` 为结构化日志调用：

```typescript
// 替换前 (moodController.ts L96):
console.error(error)

// 替换后:
logger.error('记录情绪失败', { 
  error: error instanceof Error ? error.message : 'unknown_error',
  userId: req.user?.userId 
})
```

需替换的位置（共 10 处）：

| 行号 | 对应方法 |
|------|---------|
| L96 | recordMood |
| L117 | getMoodList |
| L127 | getWeeklyReportHandler |
| L216 | updateMoodHandler |
| L239 | deleteMoodHandler |
| L256 | getMoodTrend |
| L272 | getMoodTypes |
| L283 | getTagsHandler |
| L304 | createTagHandler |
| L321 | getMoodAnalysisHandler |

---

### P1-2. getMoodTypes / getTagsHandler / createTagHandler 返回格式不一致

| 项目 | 详情 |
|------|------|
| **问题类型** | API 契约不一致 |
| **故障位置** | `mood_health_server/src/controllers/moodController.ts` L270, L281, L298-L301 |
| **严重程度** | ⚠️ P1 隐性风险 |
| **影响范围** | 前端类型定义与实际返回数据不匹配 |

**故障原因分析：**

这三个方法直接返回 `{ code: 0, data: ..., message: '...' }` 而非使用 `apiSuccess()` 包装。error 场景返回 `{ code: 500, message: '...' }` 而非 `apiFailure(500, '...')`，code 字段直接用 HTTP 状态码而非业务错误码（`API_ERROR_CODES.INTERNAL_ERROR = 1500`）。

此外，前端 `MoodTypeEnum` 类型定义为 `{ value: string, label: string }`，但后端返回的是 `{ id, name, icon, category }`，类型不匹配。

**修复建议：**

统一使用 `apiSuccess` / `apiFailure`：

```typescript
// mood_health_server/src/controllers/moodController.ts

// getMoodTypes (L261-L275)
export const getMoodTypes = async (req: AuthRequest, res: Response) => {
  try {
    const emotionTypes = await moodService.listEmotionTypes()
    const formattedTypes = emotionTypes.map((type) => ({
      id: type.id,
      name: type.name,
      icon: type.icon,
      category: type.category,
    }))
    res.json(apiSuccess(formattedTypes, '获取成功'))
  } catch (error) {
    logger.error('获取情绪类型失败', { error })
    res.status(500).json(apiFailure(500, '服务器错误'))
  }
}

// getTagsHandler (L277-L286)
export const getTagsHandler = async (req: AuthRequest, res: Response) => {
  try {
    const userId = req.user!.userId
    const tags = await moodService.listTags(userId)
    res.json(apiSuccess(tags, '获取成功'))
  } catch (error) {
    logger.error('获取标签失败', { error })
    res.status(500).json(apiFailure(500, '服务器错误'))
  }
}

// createTagHandler (L288-L307)
export const createTagHandler = async (req: AuthRequest, res: Response) => {
  try {
    const userId = req.user!.userId
    const { name } = req.body
    if (!name || typeof name !== 'string') {
      return res.status(400).json(apiFailure(400, '标签名称不能为空'))
    }
    const tag = await moodService.createOrGetTag(name.trim(), userId)
    res.status(201).json(apiSuccess(tag, '创建成功'))
  } catch (error) {
    logger.error('创建标签失败', { error })
    res.status(500).json(apiFailure(500, '服务器错误'))
  }
}
```

---

### P1-3. validateRequest 中间件返回格式不统一

| 项目 | 详情 |
|------|------|
| **问题类型** | API 响应格式不统一 |
| **故障位置** | `mood_health_server/src/middleware/validateRequest.ts` L7 |
| **严重程度** | ⚠️ P1 隐性风险 |
| **影响范围** | 所有带参数校验的路由，验证失败时错误详情丢失 |

**故障原因分析：**

```typescript
// 当前实现
return res.status(400).json({ code: 400, message: '参数验证失败', details: errors.array() })
```

- `code: 400` 是 HTTP 状态码，但其他接口失败时 `code` 是业务错误码（如 `API_ERROR_CODES.BAD_REQUEST = 1001`）
- 前端 `unwrapResponse` 会丢弃 `details` 字段，用户只看到"参数验证失败"而不知道具体哪个字段有问题

**修复建议：**

```typescript
// mood_health_server/src/middleware/validateRequest.ts
import { validationResult } from 'express-validator'
import { Request, Response, NextFunction } from 'express'
import { API_ERROR_CODES, apiFailure } from '../utils/apiResponse'

export const validateRequest = (req: Request, res: Response, next: NextFunction) => {
  const errors = validationResult(req)
  if (!errors.isEmpty()) {
    const messages = errors.array().map((e) => e.msg).join('; ')
    return res.status(400).json(
      apiFailure(API_ERROR_CODES.BAD_REQUEST, `参数验证失败: ${messages}`)
    )
  }
  next()
}
```

---

### P1-4. rolePermissions 硬编码映射表未被使用 — 死代码

| 项目 | 详情 |
|------|------|
| **问题类型** | 死代码 / 维护风险 |
| **故障位置** | `mood_health_server/src/middleware/auth.ts` L71-L194 |
| **严重程度** | ⚠️ P1 隐性风险 |
| **影响范围** | 无直接影响，但存在维护隐患 |

**故障原因分析：**

`auth.ts` 中定义了 124 行的 `rolePermissions` 硬编码映射表，但 `requirePermission` 中间件实际查询的是数据库 `accessRepository.hasPermission()`。硬编码的 `rolePermissions` 从未被任何中间件使用。

更危险的是，它的权限码与数据库种子数据中的权限码不一致：

| auth.ts 硬编码 | coreSeed.ts 数据库 |
|---------------|-------------------|
| `mood.record.read` | `mood.record.read_own` |
| `mood.record.update` | `mood.record.update_own` |
| `mood.record.delete` | `mood.record.delete_own` |

如果未来有人误用此映射表，将导致权限检查逻辑错误。

另外，`requireRole` 中间件（L317-L344）也未被任何路由使用。

**修复建议：**

方案 A（推荐）— 删除 `rolePermissions` 和 `requireRole`，因为它们完全未被使用，且与实际数据库权限体系不一致。

方案 B — 保留但注释说明这是"文档参考"用途，并添加 `@deprecated` 标记，将权限码与数据库种子数据对齐。

---

### P1-5. 前端 guards.ts 角色权限表与后端数据库不一致

| 项目 | 详情 |
|------|------|
| **问题类型** | 前后端权限定义不一致 |
| **故障位置** | 前端 `src/router/guards.ts` L12-L53 vs 后端种子数据 `mood_health_server/src/db/seeds/coreSeed.ts` L67-L81 |
| **严重程度** | ⚠️ P1 隐性风险 |
| **影响范围** | 前端路由守卫可能误判用户权限 |

**故障原因分析：**

前端 `guards.ts` 的 `rolePermissions` 给 `super_admin` 和 `admin` 分配了 `user.manage`，但后端种子数据中 **没有 `admin` 角色 code**（只有 `student`、`counselor`、`super_admin`）。`admin` 角色在前端被当作独立角色处理，但在数据库层面不存在。

虽然前端路由守卫只控制页面可见性，API 调用仍由后端鉴权，但前端显示不该显示的页面链接会给用户造成困惑。

**修复建议：**

将前端 `guards.ts` 中的 `rolePermissions` 与后端 `coreSeed.ts` 中的 `ROLE_PERMISSION_CODES` 对齐，移除 `admin` 角色或将其映射到 `super_admin` 的子集。

---

### P1-6. App.vue onMounted 中无条件调用 moodStore.fetchMoodList

| 项目 | 详情 |
|------|------|
| **问题类型** | 初始化时机问题 |
| **故障位置** | `src/App.vue` L116-L121 |
| **严重程度** | ⚠️ P1 隐性风险 |
| **影响范围** | token 过期时 App 初始化就触发 401 → 跳转登录页 |

**故障原因分析：**

```typescript
onMounted(() => {
  if (userStore.isLoggedIn) {
    moodStore.fetchMoodList({ page: 1, size: 10 })
  }
})
```

`isLoggedIn` 的判断条件是 `!!token.value && !!user.value`。`App.vue` 挂载时，`userStore.init()` 在 router guard 中异步执行，此时 `user.value` 可能尚未获取到。如果 token 存在但 `fetchUserInfo` 尚未完成，`isLoggedIn` 为 `false`，不会触发请求——这是安全的。但如果 token 已过期，`fetchMoodList` 请求会触发 401 → 前端响应拦截器自动清除 token 并跳转到 `/login`，在 App 初始化时这可能造成意外的页面跳转。

**修复建议：**

增加更严格的判断条件，确保 `user.value` 已加载：

```typescript
// src/App.vue L116-L121
onMounted(async () => {
  // 先确保用户信息已加载
  if (userStore.token && !userStore.user) {
    await userStore.fetchUserInfo()
  }
  if (userStore.isLoggedIn) {
    moodStore.fetchMoodList({ page: 1, size: 10 })
  }
})
```

---

## 三、💡 优化建议（P2）

> 不阻断功能，但属于规范缺陷，建议排期修复。

---

### P2-1. 加密模块启动时可能崩溃

| 项目 | 详情 |
|------|------|
| **问题类型** | 模块级副作用 |
| **故障位置** | `mood_health_server/src/utils/encryption.ts` L5-L7 |
| **严重程度** | 💡 P2 优化建议 |

**问题：**

```typescript
const ENCRYPTION_KEY = process.env.ENCRYPTION_KEY
if (!ENCRYPTION_KEY) {
  throw new Error("ENCRYPTION_KEY environment variable is required")
}
```

在模块顶层抛出异常。如果 `.env` 缺少该变量，`require` 该模块即导致进程崩溃。

**修复建议：**

改为懒加载校验，在首次调用 `encrypt/decrypt` 时才检查：

```typescript
// encryption.ts
let cachedKey: Buffer | null = null

function getKey(): Buffer {
  if (cachedKey) return cachedKey
  const keyHex = process.env.ENCRYPTION_KEY
  if (!keyHex) {
    throw new Error('ENCRYPTION_KEY environment variable is required')
  }
  cachedKey = Buffer.from(keyHex, 'hex')
  if (cachedKey.length !== 32) {
    throw new Error(`Invalid encryption key length. Expected 32 bytes, got ${cachedKey.length} bytes`)
  }
  return cachedKey
}
```

---

### P2-2. moodService 使用 require() 动态加载加密模块

| 项目 | 详情 |
|------|------|
| **问题类型** | 不规范的模块加载 |
| **故障位置** | `mood_health_server/src/services/moodService.ts` L125-L137 |
| **严重程度** | 💡 P2 优化建议 |

**问题：**

默认加密函数使用 `require('../utils/encryption')` 动态加载，绕过 TypeScript 类型检查和 tree-shaking。

**修复建议：**

改为顶层 `import`：

```typescript
// moodService.ts 顶部添加
import { encryptField, decryptField } from '../utils/encryption'

// 然后删除 L125-L137 的 fallback require 逻辑，直接使用 import 的函数：
const encryptFieldFn = dependencies.encryptField ?? encryptField
const decryptFieldFn = dependencies.decryptField ?? decryptField
```

---

### P2-3. cache.ts 使用 console.warn 和 console.log

| 项目 | 详情 |
|------|------|
| **问题类型** | 日志规范 |
| **故障位置** | `mood_health_server/src/utils/cache.ts` L14, L26, L41, L44, L55, L97, L101 |
| **严重程度** | 💡 P2 优化建议 |

**问题：**

所有 Redis 缓存操作失败时使用 `console.warn`/`console.log`，应统一使用 `logger.warn`。

**修复建议：**

```typescript
// 在 cache.ts 顶部添加
import logger from './logger'

// 全局替换：
// console.warn(...) → logger.warn(...)
// console.log(...) → logger.info(...)
```

---

### P2-4. 前端 moodRecordStore 走旧版 API 路径

| 项目 | 详情 |
|------|------|
| **问题类型** | 使用旧版 API 路径 |
| **故障位置** | `src/stores/moodRecordStore.ts` L759-L778 |
| **严重程度** | 💡 P2 优化建议 |

**问题：**

前端 `submitRecord()` 始终走旧版 `moodType` + `moodRatio` 路径，从未使用新版 `emotions` 数组路径。新版 `emotions` 路径支持每个情绪独立设置强度，更适合多情绪组合场景。当前逻辑将多个情绪类型共享同一个强度值。

**修复建议：**

将 `submitRecord` 改为使用新版 `emotions` 路径：

```typescript
// src/stores/moodRecordStore.ts
const submitRecord = async () => {
  isSubmitting.value = true
  try {
    const safeMoodTypes = selectedMoodTypes.value.length > 0
      ? [...selectedMoodTypes.value]
      : ['neutral']
    const safeIntensity = Math.min(10, Math.max(1, Math.round(intensity.value || 5)))

    // 使用新版 emotions 数组路径
    const emotions = safeMoodTypes.map((typeId, index) => ({
      emotionTypeId: typeId, // 后端通过 code 匹配，这里需要先查 emotionType 的 id
      intensity: safeIntensity,
      isPrimary: index === 0,
    }))

    const payload = {
      emotions,
      event: moodContent.value.trim() || '',
      trigger: selectedTriggers.value.join(','),
      tags: Array.from(new Set([...selectedTags.value, ...selectedTriggers.value])),
      tagIds: [], // 如需要可先查询 tag id
    }

    await submitMoodRecord(payload)
    // ...
  }
}
```

> 注意：此改动需要配套修改后端 `moodController.recordMood()` 中的 `emotions` 路径，使其能通过 `code` 字段（而非仅 `emotionTypeId`）匹配情绪类型。或者前端先通过 `getMoodTypes` API 获取情绪类型 ID 映射。

---

### P2-5. moodController 中 recordMood 函数过长

| 项目 | 详情 |
|------|------|
| **问题类型** | 代码可维护性 |
| **故障位置** | `mood_health_server/src/controllers/moodController.ts` L9-L98 |
| **严重程度** | 💡 P2 优化建议 |

**问题：**

`recordMood` 函数约 90 行，包含两套完全不同的处理逻辑（新版 `emotions` 路径 + 旧版 `moodType` 路径），建议拆分。

**修复建议：**

拆分为两个独立的 handler 函数：

```typescript
// 新版 emotions 路径
const recordMoodWithEmotions = async (req: AuthRequest, res: Response) => { /* ... */ }

// 旧版 moodType 路径（标记为 @deprecated）
const recordMoodLegacy = async (req: AuthRequest, res: Response) => { /* ... */ }

export const recordMood = async (req: AuthRequest, res: Response) => {
  if (req.body.emotions && Array.isArray(req.body.emotions) && req.body.emotions.length > 0) {
    return recordMoodWithEmotions(req, res)
  }
  return recordMoodLegacy(req, res)
}
```

---

## 四、核心流程闭环验证

**注册 → 登录 → 创建情绪数据** 完整链路分析：

```
┌──────────────┐      ┌──────────────┐      ┌──────────────────┐
│  1. 注册       │─────▶│  2. 登录       │─────▶│  3. 创建情绪数据    │
│  POST /api/   │      │  POST /api/  │      │  POST /api/     │
│  auth/register│      │  auth/login  │      │  moods/record   │
└──────────────┘      └──────────────┘      └──────────────────┘
       │                      │                       │
       ▼                      ▼                       ▼
   🚨 P0-2 + P0-3          ✅ 正常                 🚨 P0-1
   DuplicateUserError     BusinessError/          58% 情绪 ID
   返回 500                HttpException           匹配失败 → 400
   注册失败时无法          有 statusCode
   正确返回错误信息         返回正确状态码
```

**结论：**

- **步骤 1（注册）**：P0-2 + P0-3 导致注册失败时返回 500 或请求挂起。修复后，重复用户名返回 409，参数错误返回 400。
- **步骤 2（登录）**：登录控制器同样使用 `throw error` 模式（P0-3），但 `BusinessError` 和 `HttpException` 都继承 `AppError` 有 `statusCode`，如果全局错误处理器能正确捕获，则返回正确状态码。建议也按 P0-3 方案修复。
- **步骤 3（创建情绪记录）**：P0-1 是最大阻断点，58% 的情绪选择会导致 400 错误。修复后所有 24 种情绪均可正常使用。

---

## 五、优先级修复清单

| 优先级 | 编号 | 问题简述 | 影响 | 文件 | 工作量 |
|:------:|------|---------|------|------|:------:|
| **P0** | P0-2 | `DuplicateUserError` 缺少 statusCode | 注册返回 500 | `userRepository.ts` L63-L68 | 小 |
| **P0** | P0-3 | authController `throw error` 未正确处理 | 注册/登录异常时 500 | `authController.ts` L20, L34 | 小 |
| **P0** | P0-4 | `req.user!` 无非空保护 | 空指针崩溃 | `moodController.ts` 多处 | 小 |
| **P0** | P0-1 | 前端情绪 ID 与数据库不匹配 | 创建数据 58% 失败 | `coreSeed.ts` L83-L94 | 中 |
| **P1** | P1-1 | `console.error` 替代 `logger.error` | 生产无错误日志 | `moodController.ts` 10 处 | 小 |
| **P1** | P1-2 | `getMoodTypes` 等返回格式不一致 | 错误码不统一 | `moodController.ts` L270, L281, L298 | 小 |
| **P1** | P1-3 | `validateRequest` 不使用业务错误码 | 验证详情丢失 | `validateRequest.ts` L7 | 小 |
| **P1** | P1-4 | `rolePermissions` 硬编码死代码 | 维护隐患 | `auth.ts` L71-L194 | 小 |
| **P1** | P1-5 | 前端 guards 权限表与后端不一致 | 路由守卫误判 | `guards.ts` L12-L53 | 中 |
| **P1** | P1-6 | App.vue 初始化时可能触发 API | 意外跳转登录页 | `App.vue` L116-L121 | 小 |
| **P2** | P2-1 | 加密模块启动时崩溃 | 部署配置错误 | `encryption.ts` L5-L7 | 小 |
| **P2** | P2-2 | `require()` 动态加载 | 类型安全 | `moodService.ts` L125-L137 | 小 |
| **P2** | P2-3 | 缓存模块使用 console | 日志不统一 | `cache.ts` 多处 | 小 |
| **P2** | P2-4 | 前端走旧版 API 路径 | 功能限制 | `moodRecordStore.ts` L759 | 中 |
| **P2** | P2-5 | 控制器函数过长 | 可维护性 | `moodController.ts` L9-L98 | 中 |

### 建议修复顺序

```
第1轮（解决注册 500）：
  P0-2 → P0-3

第2轮（解决创建数据失败）：
  P0-1（需重新 seed 数据库）

第3轮（防御性加固）：
  P0-4 → P1-1 → P1-2 → P1-3

第4轮（清理维护）：
  P1-4 → P1-5 → P1-6 → P2-1 → P2-2 → P2-3

第5轮（架构优化）：
  P2-4 → P2-5
```

---

> 报告完毕。以上所有问题均附带故障位置、原因分析、复现步骤和修复代码示例。请按优先级顺序逐项修复，每修复一项后进行验证测试。