# 端点清理报告（A/C 类冗余路由移除）

> 审计依据：`反向API审计_孤儿端点.md`（2026-08-09）
> 执行日期：2026-08-09
> 目标：移除反向审计「优先级 1（高）」点名的**重复 / 冗余写路由**；保留主干链路与存在真实依赖的端点。

## 一、本次实际移除的路由（4 个文件，共 6 条端点）

### 1. `src/routes/auditRoutes.ts` —— 删除重复端点
- **删除** `GET /api/audit/all`（与 `GET /api/audit/operation-logs` 完全相同 handler `getOperationLogsHandler`，纯冗余）。
- 保留 `GET /api/audit/operation-logs`。

### 2. `src/routes/courseRoutes.ts` —— 删除冗余公开写路由（C 类）
- **删除** `POST /api/courses`、`PUT /api/courses/:id`、`DELETE /api/courses/:id`。
- 保留 `GET /api/courses`、`GET /api/courses/:id`（前端课程浏览正常调用）。
- 说明：课程写操作由 `/api/admin/courses`（`managementRoutes`，复用同一批 controller 函数）承担，公开写路由无人调用。
- 同步移除失效 import：`createCourse / updateCourse / deleteCourse`、`authenticate`、`requirePermission`、`auditOperation`。

### 3. `src/routes/musicRoutes.ts` —— 删除冗余公开写路由（C 类）
- **删除** `POST /api/music`、`DELETE /api/music/:id`。
- 保留 `GET /api/music`、`GET /api/music/:id`、`PUT /api/music/:id`（前端 `src/api/admin.ts` 调用 PUT 编辑）。
- 同步移除失效 import：`createMusic`、`deleteMusic`。

### 4. `src/scripts/test_role_permissions.ts`（权限冒烟脚本）—— 同步整改
- 场景 5：`/api/audit/all` → `/api/audit/operation-logs`（fallback 列表与文案一并更新）。
- 场景 6：`POST /api/music/create` → `PUT /api/music/1`，断言改为「非 403（拥有 music.manage 权限）」，保持 music.manage 权限冒烟仍然有效。

## 二、本次保留、未移除的端点（含原因）

### 保留 `GET /api/moods/analysis`（反向审计曾标记「疑似可删」）
- 依赖核查发现：`scripts/release-smoke.ps1:63` 在**发版冒烟流程**中直接调用 `GET /api/moods/analysis?range=week`；`docs/` 下多处（`SQLITE_RELEASE_*`、`tech-design/04-api-inventory.md:115/219`、`性能测试建议报告`）引用该端点。
- 反向审计本身对该端点的措辞是「复核是否过期（疑似可删）」，并非确定删除。鉴于存在发版工具链 + 文档依赖，本次**保留**，避免破坏 release 流水线。
- 建议后续单独迁移：把 `release-smoke.ps1` 与文档切到 `/api/moods/insight` 或 `/api/mood-analyses`，再删除该端点。

### 其余 21 个孤儿端点（A 类未接线功能、B 类 prompts 整模块、C 类中已保留的 GET）按反向审计结论暂不动
- 多为 admin / 内部 / 待开发功能，**非 bug**。本次仅处理优先级 1 中「确定冗余」的子集。

## 三、验证

| 验证项 | 命令 | 结果 |
|---|---|---|
| 后端类型检查 | `npx tsc --noEmit -p tsconfig.json` | `EXIT=0`，无类型 / 悬空引用错误（含 `test_role_permissions.ts`） |
| 路由冒烟测试 | `npx jest tests/unit/featureRoutes.test.ts --runInBand` | `6/6 PASS`（`GET /api/music`、`GET /api/courses` 等 kept 路由正常返回非 503） |

依赖核查确认：
- 无 jest 测试直接覆盖被删的 POST/PUT/DELETE 路由（`featureRoutes.test.ts` 仅做 GET）。
- `tests/unit/moodControllerContract.test.ts` 直接 `import { getMoodAnalysisHandler }` —— 该 handler **已保留**（未删），单测不受影响。
- `managementRoutes.ts` 复用 `createCourse / updateCourse / deleteCourse` 作为 `/api/admin/courses` 写操作 —— 移除公开写路由不孤儿这些 handler。

## 四、残留与后续建议

1. **文档待同步（非阻断）**：`docs/tech-design/04-api-inventory.md` 第 668 行 `GET /api/audit/all` 及课程/音乐 POST/PUT/DELETE 条目已过时，建议更新或标注「已废弃」。
2. **死代码（可选）**：`musicController.ts` 的 `createMusic` / `deleteMusic` 在移除路由后已无任何引用（无害导出），如需彻底清理可删除；`getMoodAnalysisHandler` 因 release-smoke.ps1 + 单测保留。
3. **可选继续项**：
   - 清理 A 类其余未接线端点（prompts 模块、feedback 管理、admin/assessments 等）—— 需先确认产品意图（待开发 vs 删除）。
   - 修复 AI 契约核对报告中的软风险（`POST /api/ai/context/analyze` 的 `suggestions` 兜底、`POST /api/mood-analyses` 无记录返 null 的调用侧类型守卫）。
   - 迁移并删除 `GET /api/moods/analysis`（见第二节）。

## 五、与正向 / 反向审计的闭环

- 正向（前端→后端缺口）：1 真 bug 已修（`/api/ai/feedback`→`/api/feedback`）。
- 反向（后端→前端孤儿）：23 → 本次移除 **6**（audit/all + courses/music 写路由），保留 `moods/analysis`（有依赖）。
- 主干链路：情绪记录、问卷、AI 三能力、咨询、知识助手、活动、帖子、放松、成就、课程/音乐浏览 —— **对齐良好，无破坏**。
