# AI 接口契约软风险修复与文档同步报告（2026-08-09）

## 一、背景
轮次⑤的「AI 接口响应字段契约核对」结论：15 个 AI 端点字段契约全部对齐、无错位；但遗留两个**非阻断软风险**，本次修复并完成上一轮 A/C 端点清理的文档收尾。

## 二、软风险①：`POST /api/ai/context/analyze` 的 `suggestions` 兜底失效（Task #30）
### 根因
`aiContextController.analyzeWithContext` 响应组装：
```ts
suggestions: result.suggestions || [result.suggestion],
```
而 `moodAnalysisService.analyzeMood` 在 AI 不返回 `suggestions` 时，按契约固定返回 `suggestions: parsed.suggestions || []`（空数组），`suggestion`（单数）则填充 `'保持积极心态'`。
JS 中**空数组为真值**，因此 `result.suggestions || [result.suggestion]` 的兜底分支**永远不触发**——当 AI 漏返回建议时，前端 `AiSuggestCard.vue` 的 `v-for` 拿到空数组，用户看不到任何建议（丢失 `'保持积极心态'` 兜底）。字段虽当前不会是 `undefined`（靠 service 巧合保证），但控制器逻辑脆弱。

### 修复（`src/controllers/aiContextController.ts`）
改为显式判断，字段永远是 `string[]`：
```ts
const safeSuggestions: string[] =
  Array.isArray(result.suggestions) && result.suggestions.length > 0
    ? result.suggestions
    : typeof result.suggestion === 'string' && result.suggestion.trim()
      ? [result.suggestion]
      : []
```
- AI 返回真实建议 → 原样返回；
- AI 漏返回（与 service 真实行为一致，`suggestions: []`、`suggestion: '保持积极心态'`）→ 退化为 `['保持积极心态']`；
- 双来源皆空 → `[]`（绝不 `undefined`）。

### 验证
- 新增 `tests/unit/controllers/aiContextController.test.ts`：3/3 PASS（覆盖上述三态）。
- 后端 `tsc --noEmit` 0 错。

## 三、软风险②：`POST /api/mood-analyses` 无记录返 `null` 的调用侧崩溃（Task #31）
### 根因
后端 `moodAnalysisController.createAnalysis` 在 `NO_RECORDS`（该周期无情绪记录）时返回 `apiSuccess(null, '该周期内无情绪记录')`（HTTP 200 + `data:null`）。
前端 `request` 的 `unwrapResponse` 返回 `payload.data`，即 `null`；但前端 `createAnalysis` 类型声明为非空 `AnalysisResponse`，`MoodAnalysis.vue` 的 `generateAnalysis` 里 `const created = await createAnalysis(...); id = created.id` 在 `created` 为 `null` 时 **TypeError 崩溃**。

### 修复
1. `src/api/moodAnalysis.ts`：`createAnalysis` 返回类型改为 `AnalysisResponse | null`（如实反映契约）。
2. `src/views/mood/MoodAnalysis.vue`：`generateAnalysis` 加 null 守卫——`created` 为空时 `ElMessage.warning('该周期内还没有情绪记录，先去记录一下心情吧')` 后 `return`，不再触碰 `created.id`。

### 验证
- 新增 `src/__tests__/components/mood/MoodAnalysis.generateAnalysis.test.ts`（`shallowMount` + mock `@/api/moodAnalysis` / `@/api/moodInsight`）：2/2 PASS（null 路径不崩溃/提前返回且不触发轮询；有效对象路径不抛错）。
- 既有 `src/__tests__/api/moodAnalysis.test.ts`：3/3 仍 PASS（模块导入未受影响）。

## 四、文档同步（完成上一轮 A/C 端点清理的收尾）
`docs/tech-design/04-api-inventory.md`：
- 删除已移除的 `GET /api/audit/all` 行（与 `operation-logs` 完全重复的冗余端点，A/C 清理时已删路由），并加注移除说明。
- 审计分组计数 2 → 1；合计 69 → 68。
- courses/music 模块数为粗粒度汇总（未逐路由枚举），保持不变。

## 五、验证汇总
| 项 | 结果 |
|---|---|
| 后端 `tsc --noEmit` | 0 错 |
| `aiContextController.test.ts` | 3/3 PASS |
| `MoodAnalysis.generateAnalysis.test.ts` | 2/2 PASS |
| `moodAnalysis.test.ts`（既有） | 3/3 PASS |

## 六、遗留（可选，未做）
- `musicController.createMusic/deleteMusic` 已成无害死导出（无路由/测试引用），可选清理。
- 迁移并删 `GET /api/moods/analysis`：须先迁 `scripts/release-smoke.ps1:63` + 同步 docs，否则破坏发版流水线（🔴 跨会话勿随意删）。
- A 类其余未接线端点（prompts 模块 / feedback 管理 / admin/assessments 等）仍需产品意图确认（待开发 vs 删除）。

## 七、用户手动项（不变）
- S1 轮换泄露 DeepSeek Key；S3 换 MySQL/Redis 强口令；密钥迁入密钥管理。
- git 提交与 .gitignore 改动暂缓（沿用会话约定）。
