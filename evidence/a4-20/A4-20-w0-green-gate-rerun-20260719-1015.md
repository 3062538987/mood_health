# A4-20 W0 绿色门复核证据（二次执行）

- 时间：2026-07-19T10:15:04.2290649+08:00
- 执行者：Codex 5.5 Medium
- 基线提交：6e36388
- 项目目录：D:\桌面\ccooddee
- 计划文件：D:\桌面\ccooddee\tasks\基于当前代码的ABCD全量优化执行计划.md
- 任务 ID：A4-20 W0 绿色门
- 依赖：A4-01～A4-19 中所有 P0
- 允许修改文件：仅 A4-20 证据/报告文件；不修改生产代码、测试实现或启动配置。

## 本轮前置状态

`git status --short` 为空。上一轮遗留的本地自动化触发记录已通过 `.gitignore` 忽略，不再污染任务清洁性判断。

## 门禁结果

| 命令 | 结果 | 证据摘要 |
| --- | --- | --- |
| `npm run typecheck:all` | 通过 | 前端 `vue-tsc --noEmit` 与后端 `tsc --noEmit` 均退出 0。 |
| `npm run build:all` | 通过但有警告 | 前端 Vite 构建与后端 `tsc` 均退出 0；仍有 `src/api/moodAnalysis.ts` 经 `src/api/mood.ts` 与 `src/stores/moodRecordStore.ts` 形成循环分块风险，以及 vendor chunk 循环/大 chunk 警告。 |
| `npm run test:run` | 失败 | Vitest：42 个测试文件通过、2 个失败；174 项通过、7 项失败。失败仍集中在 `src/__tests__/views/login-success-feedback.test.ts` 和 `src/__tests__/views/home-onboarding.test.ts`。 |
| `npm --prefix mood_health_server run test:stable` | 通过但有风险 | Jest：45 套通过、218 项通过；结束后仍提示存在未关闭异步句柄，建议用 `--detectOpenHandles` 定位。 |
| `python -m pytest -q`（在 `mood_health_ai_service`） | 失败 | Python 测试收集失败：缺少 `fastapi` 与 `pydantic_settings`，并提示未知 `asyncio_mode` 配置。 |
| `python -m ruff check .`（在 `mood_health_ai_service`） | 失败 | 当前 Python 环境缺少 `ruff` 模块。 |
| `python -m mypy app`（在 `mood_health_ai_service`） | 失败 | 当前 Python 环境缺少 `mypy` 模块。 |
| `npm run doctor:strict` | 失败 | doctor 发现缺少 `start-project.ps1`，并判定 `start-project.ps1/start-project.sh` 启动脚本缺失；前端 3001、Node 3000 未运行为 warning。 |
| `npm run lint:check` | 失败 | ESLint 仍扫描 `playwright-report/trace/assets/*.js` 等生成产物，报告 3115 个错误。 |
| `git diff --check` | 通过 | 未发现空白错误。 |

## 退回建议

- 退回 A4-18：启动合同仍不满足，`doctor:strict` 缺 `start-project.ps1/start-project.sh`，Python 测试/质量依赖也未能复现。
- 退回 A4-08 或测试底座维护者：`lint:check` 应排除 Playwright 生成报告目录，或清理生成产物后重新确认。
- 退回 B/D 或登录/首页测试拥有者：登录成功后实际跳转 `/guide`，测试期望默认 `/` 或保留安全 redirect；首页首条记录卡实际 class 为 `.onboarding-card`，测试期望 `.first-record-onboarding`。
- 记录构建风险：Rollup circular chunk 警告仍存在，涉及 mood analysis API re-export 与 store 引用路径。

## 放行结论

A4-20 二次执行仍不通过，W0 不能标记绿色。当前代码已具备通过类型检查、构建和后端单测的基础，但前端单测、Python/FastAPI 质量门、doctor strict、lint check 均未通过。按 Codex 5.5 Medium 边界，本任务只记录证据并退回对应负责模型，不直接修复。
