根据多个对话中提到的“回家后需要优化的内容”，我为你汇总了一份全面的任务清单，去除了重复项，并按类别整理。请按顺序逐步完成。

---

## 📋 回家后需完成的任务清单

### 1️⃣ 环境准备
- [ ] **安装 Node.js**（LTS 版本，如 18.x 或 20.x）  
  - 验证：`node -v`、`npm -v`
- [ ] **安装 SQL Server**（Developer 或 Express 版）  
  - 确保服务运行，启用 TCP/IP 协议，设置混合验证，启用 sa 账户
  - 或使用 Docker 快速启动（`docker run ...`）
- [ ] **安装 Redis**（如需缓存功能）  
  - 下载安装或使用 Docker 运行，确保服务启动（默认端口 6379）
- [ ] **创建日志目录**  
  - 在后端项目根目录下创建 `logs` 文件夹（用于存放日志文件）

---

### 2️⃣ 项目依赖安装
- [ ] **后端依赖安装**  
  - 进入 `mood-health-server` 目录，运行 `npm install`  
  - 这将安装包括 `express-validator`、`ioredis`、`winston`、`morgan`、`compression`、`express-rate-limit` 等所有新增依赖
- [ ] **前端依赖安装**  
  - 进入项目根目录 `mood-health-web`，运行 `npm install`

---

### 3️⃣ 数据库配置
- [ ] **创建数据库**  
  - 在 SQL Server 中创建数据库，例如 `mood_health_db`（可自定义）
- [ ] **配置环境变量**  
  - 在后端目录下创建或修改 `.env` 文件，填入正确的数据库连接信息：
    ```
    DB_HOST=localhost
    DB_PORT=1433
    DB_USER=sa
    DB_PASSWORD=你的密码
    DB_NAME=mood_health_db
    JWT_SECRET=your-secret-key
    REDIS_HOST=127.0.0.1
    REDIS_PORT=6379
    # 如有 Redis 密码则添加
    ```
- [ ] **运行数据库初始化脚本**  
  - 构建后端：`npm run build`  
  - 执行脚本创建表结构：`node dist/scripts/initDb.js`  
    （该脚本应包含所有模块的表：users、moods、activities、posts、comments、questionnaires、questions、user_answers 等）
  - 或者逐个运行分脚本（如 `createTreeHoleTables.ts`、`createQuestionnaireTables.ts`）

---

### 4️⃣ 代码构建
- [ ] **后端编译**  
  - 在 `mood-health-server` 下运行 `npm run build`，生成 `dist` 文件夹
- [ ] **前端构建**（如需部署）  
  - 在项目根目录下运行 `npm run build`，生成 `dist` 文件夹（用于静态部署）

---

### 5️⃣ 功能测试与验证
- [ ] **启动后端服务**  
  - 开发模式：`npm run dev`（用于测试）
  - 生产模式：`npm start`（运行编译后的代码）
- [ ] **测试各模块 API**（使用 Postman 或浏览器）  
  - **认证模块**：注册、登录、获取当前用户  
  - **情绪模块**：记录、列表、周报、修改/删除  
  - **活动模块**：列表、详情、报名、我的活动  
  - **树洞模块**：发帖、评论、点赞、分页列表、详情  
  - **问卷模块**：获取问卷列表/详情、提交答案、个人历史  
- [ ] **验证新增功能**  
  - **数据验证**：尝试提交非法数据，检查返回的错误信息是否正确  
  - **缓存功能**：多次请求活动列表，观察第二次响应是否变快（Redis 缓存生效）  
  - **限流功能**：快速多次调用登录接口，触发限流返回 429  
  - **压缩功能**：检查响应头是否包含 `Content-Encoding: gzip`  
  - **日志记录**：查看 `logs` 目录下是否生成 `error.log` 和 `combined.log`，控制台是否输出请求日志  
- [ ] **前端集成测试**  
  - 启动前端 `npm run dev`，登录并操作树洞、问卷等新功能，确保前后端联调正常

---

### 6️⃣ 代码优化与完善
- [ ] **错误处理**  
  - 检查所有控制器是否使用 `try/catch` 并调用 `next(error)`，统一错误处理中间件是否正常工作
- [ ] **输入验证**  
  - 确保所有 POST/PUT 接口都添加了 `express-validator` 规则，并使用了 `validateRequest` 中间件
- [ ] **认证中间件**  
  - 确认需要登录的接口（如提交问卷、报名活动）已正确添加 `auth` 中间件
- [ ] **安全性检查**  
  - 所有数据库查询使用参数化查询（防止 SQL 注入）  
  - 敏感信息（密码）已加密存储  
  - 考虑为所有 API 添加速率限制（至少登录接口已加）
- [ ] **性能优化**  
  - 检查分页查询是否添加 `ORDER BY` 和索引  
  - 考虑为频繁查询的接口（如活动列表、情绪类型列表）添加 Redis 缓存（活动列表已加，可扩展）  
  - 对于大表查询，评估是否需要添加索引

---

### 7️⃣ 部署准备（如需要上线）
- [ ] **配置 PM2 进程管理**  
  - 全局安装 PM2：`npm install -g pm2`  
  - 使用 `ecosystem.config.js` 启动：`pm2 start ecosystem.config.js`  
  - 设置开机自启：`pm2 startup` 和 `pm2 save`
- [ ] **配置 Nginx 反向代理**  
  - 参考 `nginx.conf.example` 配置，将前端静态文件托管，并将 `/api` 请求代理到后端端口
- [ ] **环境变量管理**  
  - 根据生产环境调整 `.env` 中的参数（如数据库密码、JWT 密钥、NODE_ENV=production）
- [ ] **监控与日志**  
  - 确保日志文件定期轮转，避免磁盘占满  
  - 可添加简单的健康检查接口 `/health`

---

### 8️⃣ 文档完善
- [ ] **API 接口文档**  
  - 整理所有接口的请求/响应格式，可使用 Swagger 或 Markdown 文档
- [ ] **代码注释**  
  - 为关键函数、复杂逻辑添加注释，方便后续维护

---

完成以上步骤后，你的项目将在新电脑上完整运行，所有新增功能（树洞、问卷、数据验证、缓存、日志、限流、压缩）均可正常工作。如果在操作中遇到任何问题，随时可以回来询问。祝你顺利！

以下是你可以直接复制给其他AI的详细任务提示词。每个提示词都包含了项目背景、技术栈、当前状态、具体要求以及注意事项。请根据你的实际需求选择使用，并在必要时补充具体的文件路径或代码片段。

---

## 通用项目背景（请在每次提问前附带）

```
我正在开发一个名为“大学生情绪健康管理平台”的Web项目，技术栈如下：
- 前端：Vue 3 + TypeScript + Vite + Pinia + Vue Router + Axios + ECharts + SCSS
- 后端：Node.js + TypeScript + Express + SQL Server + JWT + bcryptjs + Redis（可选）
- 数据库：SQL Server，已设计 users、moods、activities、posts 等表

项目已具备基础功能：情绪记录、情绪档案、情绪周报、树洞发帖/评论/点赞、活动报名、问卷提交等。现在需要根据优化建议，逐步实现新功能或改进现有模块。

请帮我实现以下具体任务，并提供完整的代码示例（前端组件、后端API、数据库操作等），并解释关键逻辑。
```

---

## P0 级任务提示词

### 任务1：情绪记录多样化（增加情绪标签和触发因素）

```
任务目标：在现有情绪记录表单中增加“情绪标签”和“触发因素”字段，让用户更详细地描述情绪。

具体要求：
1. 数据库修改：在 moods 表中增加两个字段：
   - tags（可选）：字符串类型，存储用户选择的标签，多个标签用逗号分隔（如“学习压力,人际关系”）。
   - trigger（可选）：字符串类型，存储触发因素，预设选项如“学习”“社交”“家庭”“健康”“其他”，可单选或自定义输入。
2. 前端修改：
   - 在情绪记录页面（新增/编辑）的表单中添加：
     - 标签多选框：提供常见情绪标签（如“焦虑”“开心”“疲惫”“平静”“愤怒”），支持多选，显示为tag样式。
     - 触发因素下拉框：包含预设选项 + “其他”选项，当选择“其他”时，显示输入框供用户自定义。
   - 提交时将这些字段一同发送到后端。
3. 后端修改：
   - 更新情绪记录的API（POST /api/moods）和更新API（PUT /api/moods/:id），接收 tags 和 trigger 字段。
   - 返回情绪列表时（GET /api/moods）也包含这些字段。
4. 界面风格：保持与现有设计一致，使用SCSS样式，确保移动端适配。

请提供：
- 数据库迁移脚本（SQL语句）修改 moods 表。
- 修改后的情绪记录组件（Vue文件）代码。
- 后端路由和控制器的更新代码（Node.js + Express）。
- 相关的TypeScript类型定义更新。
```

### 任务2：情绪趋势分析深度化（周/月/季度图表）

```
任务目标：在情绪分析页面增加按周、月、季度查看情绪趋势的图表，并给出简单的文字总结。

具体要求：
1. 前端：
   - 在现有情绪分析页面添加时间范围切换按钮（周/月/季度），默认显示最近一周。
   - 使用ECharts绘制折线图，X轴为日期，Y轴为情绪强度（平均值），可以显示多种情绪类型（如快乐、悲伤）的曲线。
   - 在图表下方显示一段自动生成的文字总结，例如“最近一周你的情绪波动较大，建议多进行放松练习。”
   - 支持从后端获取对应时间范围的数据。
2. 后端：
   - 新增API：GET /api/moods/trend?range=week|month|quarter，返回指定时间范围内每天的情绪统计数据（如平均强度、主要情绪类型等）。
   - 根据时间范围计算起始日期和结束日期，从moods表中分组查询。
3. 数据格式示例：
   {
     "labels": ["2025-03-01", "2025-03-02", ...],
     "datasets": [
       { "name": "快乐", "data": [3, 4, 2, ...] },
       { "name": "焦虑", "data": [1, 2, 3, ...] }
     ],
     "summary": "本周情绪平稳，但周末略有下降。"
   }

请提供：
- 前端图表组件代码（Vue + ECharts），包括时间切换逻辑。
- 后端新增API的路由、控制器和数据库查询（SQL Server）。
- 总结文字的生成逻辑（可先基于简单规则，如比较平均值）。
```

### 任务3：响应式布局与移动端适配

```
任务目标：确保所有页面在手机、平板等移动设备上显示正常，交互友好。

具体要求：
1. 全局样式：
   - 使用CSS媒体查询，为不同屏幕宽度设置合理的字体大小、间距、布局。
   - 在移动端，顶部导航栏改为汉堡菜单（折叠），侧边栏隐藏或改为底部导航（如果适用）。
   - 确保按钮、输入框等点击区域足够大（至少44x44px）。
2. 各个页面检查：
   - 情绪记录表单：输入框宽度自适应，按钮纵向排列。
   - 图表页面：图表容器自适应，横坐标文字倾斜或省略。
   - 树洞帖子列表：卡片式布局，左右留白适中。
   - 个人中心：列表项可点击区域扩大。
3. 测试：至少在iPhone SE（375px）和iPad（768px）尺寸下预览效果。

请提供：
- 全局SCSS文件中的媒体查询示例。
- 对关键组件（如导航栏、表单、卡片）的移动端样式修改建议。
- 如果现有布局有重大问题，请指出并给出修改方案。
```

### 任务4：统一视觉风格（温暖、专业色调）

```
任务目标：为整个平台定义一套温暖、专业的色彩方案，并统一应用到所有页面组件中。

具体要求：
1. 主色调选择：采用柔和的蓝绿色（例如 #6AB0A5）或暖橙色（例如 #F3A683），辅助色为浅灰、米白等。
2. 修改SCSS变量：在全局变量文件中定义颜色变量，如 $primary-color、$secondary-color、$background-light、$text-color等。
3. 应用：
   - 按钮、链接、图标使用主色调。
   - 卡片背景为白色或浅灰色，带轻微圆角和阴影。
   - 字体颜色适中，避免纯黑。
4. 确保对比度符合可访问性标准（文本与背景对比度至少4.5:1）。
5. 提供一套简洁的UI组件样式规范，包括按钮、输入框、卡片、标签等。

请提供：
- 全局SCSS变量定义文件。
- 修改后的常用组件样式代码（如Button.vue、Input.vue，如果已有则提供样式覆盖建议）。
- 示例：一个典型页面（如情绪记录页）在应用新风格后的效果说明。
```

---

## P1 级任务提示词

### 任务5：集成常用心理量表（SDS/SAS）

```
任务目标：在“提升计划”模块中增加抑郁自评量表（SDS）和焦虑自评量表（SAS），用户完成测试后可查看结果和解读。

具体要求：
1. 数据库设计：
   - 新增表 questionnaires：存储量表基本信息（id, title, description, type等）。
   - 新增表 questions：存储问题（id, questionnaire_id, question_text, options_json等）。
   - 新增表 user_assessments：存储用户测评记录（id, user_id, questionnaire_id, score, result_text, created_at）。
2. 前端：
   - 创建量表列表页，展示可用的量表（SDS、SAS）。
   - 点击量表开始测试，进入答题页：每页显示一道题，单选选项（如“没有或很少时间”等4级），进度条显示。
   - 提交答案后，后端计算总分并返回结果和解读，前端展示结果页。
3. 后端：
   - API：GET /api/questionnaires 获取量表列表。
   - API：GET /api/questionnaires/:id/questions 获取某个量表的所有问题。
   - API：POST /api/assessments 提交答案（接收 questionnaire_id 和 answers 数组），计算得分并存储，返回结果。
4. 计分规则：SDS和SAS有标准计分方式（反向计分题需处理），可硬编码在后端逻辑中。
5. 结果解读：根据分数区间给出不同文字建议（如正常、轻度、中度、重度）。

请提供：
- SQL建表语句。
- 量表数据插入语句（SDS和SAS的问题和选项）。
- 后端API路由、控制器、服务层代码。
- 前端答题组件和结果展示组件（Vue）。
- 解释计分规则和结果判断逻辑。
```

### 任务6：树洞社区增强（点赞/评论提醒、话题分类）

```
任务目标：增强树洞社区的互动性，增加评论提醒和帖子分类功能。

具体要求：
1. 数据库修改：
   - 在 posts 表中增加 category 字段（字符串），存储话题分类，如“学业”“情感”“生活”“其他”。
   - 新增 notifications 表：存储用户通知（id, user_id, type, content, is_read, created_at, link等）。
2. 前端：
   - 帖子发布表单增加分类选择下拉框。
   - 帖子列表页增加分类筛选标签。
   - 在个人中心增加“消息通知”入口，点击进入列表页，显示评论回复、点赞等通知。
3. 后端：
   - 当用户评论或点赞时，生成通知并存储到 notifications 表。
   - API：GET /api/notifications 获取当前用户的通知列表（分页）。
   - API：PUT /api/notifications/read 标记通知为已读。
4. 评论提醒内容示例：“张三 评论了你的帖子：《xxxx》”。

请提供：
- 数据库表修改和新建的SQL。
- 后端通知生成逻辑（在评论和点赞接口中触发）。
- 前端通知组件和相关API调用。
```

### 任务7：放松中心内置音频（呼吸引导、白噪音）

```
任务目标：在放松中心添加可播放的音频文件，帮助用户放松。

具体要求：
1. 音频资源：暂时使用免费可用的音频文件（如呼吸引导、雨声、海浪声），可放在 public/audio 目录下。
2. 前端：
   - 在放松中心页面新增“音频放松”板块，展示多个音频卡片（封面图、标题、播放按钮）。
   - 点击播放按钮，页面底部出现全局音频播放器（包含播放/暂停、进度条、音量控制）。
   - 支持后台播放（切换页面不中断），使用HTML5 Audio API。
3. 后端：无需新增API，音频文件作为静态资源提供。
4. 体验优化：播放器可最小化，不影响浏览其他内容。

请提供：
- 前端音频播放器组件代码（Vue）。
- 如何将音频文件组织并引用。
- 播放控制逻辑（暂停、切换、进度更新）。
```

### 任务8：个人中心数据概览（情绪日历/热力图）

```
任务目标：在个人中心页面展示情绪记录日历热力图，点击日期查看当天记录。

具体要求：
1. 前端：
   - 使用ECharts日历图（calendar + heatmap）展示最近3个月每天的情绪平均强度或主要情绪类型。
   - 颜色渐变表示强度（如绿色到红色），或不同颜色代表不同情绪类型。
   - 点击某个日期，弹出或跳转到当天的情绪记录详情。
2. 后端：
   - 新增API：GET /api/moods/calendar?start=YYYY-MM-DD&end=YYYY-MM-DD，返回指定日期范围内每天的情绪统计数据（如平均强度、主要情绪）。
3. 数据格式示例：
   [
     { "date": "2025-03-01", "value": 3, "type": "快乐" },
     { "date": "2025-03-02", "value": 2, "type": "焦虑" }
   ]

请提供：
- 后端API代码。
- 前端日历图组件代码，包括日期点击事件。
```

---

## P2 级任务提示词（可选）

### 任务9：个性化情绪建议（基于历史数据）

```
任务目标：根据用户的情绪记录历史，在情绪分析页面或首页推送个性化的放松方式或科普文章。

具体要求：
1. 规则设计：初期可采用简单规则，如：
   - 如果最近一周焦虑记录次数>=3，推荐呼吸练习或白噪音。
   - 如果最近一周情绪强度波动大，推荐正念冥想。
   - 如果情绪持续低落，推荐量表测评或树洞倾诉。
2. 后端：
   - 新增API：GET /api/recommendations，分析用户最近情绪数据，返回推荐列表（包含类型、标题、链接、图片等）。
3. 前端：
   - 在情绪分析页面下方或首页添加“为你推荐”卡片区域，展示2-3条建议。

请提供：
- 后端推荐逻辑代码。
- 前端推荐卡片组件。
```

### 任务10：心理科普知识库（分类文章）

```
任务目标：建立心理科普文章模块，用户可浏览、搜索和阅读文章。

具体要求：
1. 数据库：
   - 新增表 articles：id, title, content, category, cover_image, views, created_at。
2. 前端：
   - 文章列表页（按分类筛选、搜索）。
   - 文章详情页（显示内容、相关文章推荐）。
3. 后端：
   - CRUD API：获取文章列表、详情、增加阅读数。
4. 初始内容：可先手动插入几篇示例文章。

请提供：
- 数据库建表SQL。
- 后端API代码。
- 前端列表和详情组件。
```

---

## 使用建议

1. **每次只提问一个任务**，避免混淆。
2. **在提问时附上相关的现有代码片段**（如现有的情绪记录组件代码、API路由等），让AI能更好地理解上下文。
3. **明确要求AI提供完整可运行的代码**，并解释关键点。
4. **测试代码时，先从简单场景开始**，逐步集成。
5. **遇到问题及时追问**，要求AI debug或优化。

希望这些提示词能帮助你高效地推进项目！